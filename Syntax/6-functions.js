'use strict';

function average(a, b) {
    return (a + b) / 2;
}

function square(x) {
    return x * x;
}

function qube(x) {
    return x * x * x;
}

function calculate(i, j) {
    let array = [];
    for(let i = 0; i <= j; i++) {
        
    }
}